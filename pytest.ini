[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: integration tests requiring external services
    e2e: end-to-end tests requiring live gateway
    regression: regression comparisons vs a baseline version
